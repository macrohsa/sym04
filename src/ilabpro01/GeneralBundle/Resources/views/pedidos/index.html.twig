{% extends '::base.html.twig' %}

{% block body -%}
    <h1>pedidos list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Sitio</th>
                <th>Fotografo</th>
                <th>Usuario</th>
                <th>Fechaalta</th>
                <th>Importe</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('pedidos_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.sitio }}</td>
                <td>{{ entity.fotografo }}</td>
                <td>{{ entity.usuario }}</td>
                <td>{{ entity.fechaAlta }}</td>
                <td>{{ entity.importe }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('pedidos_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('pedidos_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('pedidos_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
